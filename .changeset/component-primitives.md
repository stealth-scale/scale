---
"@stealthscale/component-primitives": patch
---

component-primitives: show every component

- One specimen per component, with the words read through the catalogue's `specimen` namespace from
  `locales/en/specimen/`.
