---
"@stealthscale/pandacss-naming": patch
---

Rename only the classes the compiler owns: a recipe-claimed class, and an atomic class whose utility
carries the separator. Leave an author's own class as written.
