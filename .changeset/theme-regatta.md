---
"@stealthscale/theme-regatta": minor
---

theme-regatta: add the theme

- A crimson product with deep blue and teal beside it, on navy after dark and on the palest navy by
  day, with sharp corners. The crimson is the primary and the keyword ink, the navy the dark page
  and the light ink, the deep blue the panel on the dark page and the secondary, and the teal the
  accent and the type ink. The dark page is written in a pale teal.
- The four colors are stated outright and `defineTheme` draws every other value from them at the
  ratios the gate measures. Each status keeps its canonical hue, and an error a shade off the
  crimson is moved a step in lightness so a destructive action is told from the primary. The
  specification runs every check with nothing skipped.
- Regatta draws sharp on every axis it states. The corners come from an eighth of a rem, and the
  focus ring is three pixels wide flush with the control. Every control, icon, tag, inset and gap is
  drawn at 95% density, with the gap inside a control from three eighths of a rem. A press is
  answered in four fifths of the foundation's time on a symmetric curve, and the shadows fall a
  quarter harder. Every heading is set extrabold and tracked tighter in a condensed system stack,
  every label semibold, and a recipe extension sets every button in capitals.
