/**
 * Publishes the transfer: two lists and the pair of controls that move rows between them.
 *
 * @remarks
 *   One component rather than a namespace of parts. What a transfer draws is fixed — two lists and
 *   two controls — and a caller who wants something else composes two listboxes.
 */

export { Transfer, type TransferProps } from "#transfer/transfer.tsx";
