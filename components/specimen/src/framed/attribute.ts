/**
 * Names the attribute a framed document carries on its root, which the kit's preset styles the
 * document by.
 *
 * @remarks
 *   The theme paints the document root in the page's background. A framed document sits inside a
 *   card on the page, and the sample it holds should sit on that card the way it does on the page,
 *   so the preset makes a root carrying this attribute see-through.
 */

/**
 * The attribute written on the root of a framed document.
 */
export const FRAMED_ATTRIBUTE = "data-framed";
