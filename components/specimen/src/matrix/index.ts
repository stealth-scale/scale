/**
 * Publishes the matrix: one captioned cell per value of an axis, and the reader that lists an
 * axis's values off a recipe.
 */

export { type Axis, captionOf, nameOf } from "#matrix/axis.ts";
export { Matrix, type MatrixProps } from "#matrix/matrix.tsx";
export { type ValueOf, valuesOf } from "#matrix/values.ts";
