/**
 * Publishes the sample: one captioned drawing of a component, and the look a container sets for
 * every sample inside it.
 */

export { type Display, DisplayProvider } from "#sample/display.ts";
export { Sample, type SampleProps } from "#sample/sample.tsx";
