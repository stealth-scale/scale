/**
 * Publishes the stage, and the widths a scene can be shown at.
 */

export { Stage, type StageProps } from "#stage/stage.ts";
export { PHONE, type StageWidth, stageWidthOf, widthsOf } from "#stage/width.ts";
